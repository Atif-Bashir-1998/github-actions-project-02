import { describe, expect, test } from 'vitest';

describe('HelloWorld', () => {
  // Test 1: Checks if the component renders the message prop correctly.
  test('sanity check', () => {

    expect(2+3).toBe(4);
  });
});